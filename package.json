{
  "name": "accessibility-checker-wrapper",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run start --prefix accessibility-checker",
    "heroku-postbuild": "npm install --prefix accessibility-checker && npm run build --prefix accessibility-checker"
  },
  "engines": {
    "node": "18.x"
  }
}
